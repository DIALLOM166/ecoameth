{% extends 'base.html.twig' %}

{% block title %}OrderItems index{% endblock %}

{% block body %}
    <h1>OrderItems index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Quantity</th>
                <th>Price</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for order_item in order_items %}
            <tr>
                <td>{{ order_item.id }}</td>
                <td>{{ order_item.quantity }}</td>
                <td>{{ order_item.price }}</td>
                <td>
                    <a href="{{ path('app_order_items_show', {'id': order_item.id}) }}">show</a>
                    <a href="{{ path('app_order_items_edit', {'id': order_item.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_order_items_new') }}">Create new</a>
{% endblock %}
