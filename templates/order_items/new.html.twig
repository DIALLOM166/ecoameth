{% extends 'base.html.twig' %}

{% block title %}Create New Order Item{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1 class="mb-4">Create New Order Item</h1>

        <div class="mb-3">
            {{ include('order_items/_form.html.twig') }}
        </div>

        <a href="{{ path('app_order_items_index') }}" class="btn btn-secondary">← Back to list</a>
    </div>
{% endblock %}
